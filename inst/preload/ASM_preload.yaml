# Top level is the module ID and the mod_type is the module short name
ASM:
  fm_yaml:   'config/formods.yaml'
  mod_yaml:  'config/ASM.yaml'
  # This stores the placeholders for word documents. Just use the placeholder
  # text in the document and what it should be substituted for in the right
  docx_ph:
    HEADERLEFT:  'left header'
    HEADERRIGHT: 'right header'
    FOOTERLEFT:  'left footer'
