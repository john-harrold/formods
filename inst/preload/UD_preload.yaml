# Top level is the module ID and the mod_type is the module short name
UD:
  fm_yaml:   'system.file(package="formods", "templates", "formods.yaml")'
  mod_yaml:  'system.file(package="formods", "templates", "UD.yaml")'
  data_source: 
    # For csv or tab file just set sheet to NULL
    file_name: 'system.file(package="formods", "test_data", "TEST_DATA.xlsx")'
    sheet:     'DATA'
    clean:     TRUE
