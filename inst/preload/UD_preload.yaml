# Top level is the module ID and the mod_type is the module short name
UD:
  fm_yaml:   'config/formods.yaml'
  mod_yaml:  'config/UD.yaml'
  data_source: 
    # For csv or tab file just set sheet to NULL
    file_name: 'file.path(getwd(), "TEST_DATA.xlsx")'
    sheet:     'DATA'
    clean:     TRUE
